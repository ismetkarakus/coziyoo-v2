---
phase: 03.2-mobile-login
plan: "02"
title: Authenticated session start + logout
subsystem: mobile, api
tags: [auth, session-start, bearer-token, logout, preflight]
key_files:
  modified:
    - apps/mobile/src/screens/HomeScreen.tsx
    - apps/mobile/App.tsx
    - apps/api/src/routes/livekit.ts
decisions:
  - "Switched from /starter/session/start to /session/start (requireAuth) — real userId in session metadata"
  - "401 from session start triggers one refresh attempt; if refresh fails, logout"
  - "HomeScreen no longer has a username TextInput — identity comes from JWT"
  - "STT/TTS/n8n preflight checks ported to authenticated /session/start so health warnings still work"
metrics:
  duration: ~10m
  completed: 2026-03-16
  tasks_completed: 3
  files_modified: 3
---

# Phase 3.2 Plan 02: Authenticated Session Start + Logout

Rewrote HomeScreen to accept `auth: AuthSession` prop and call `POST /v1/livekit/session/start` with `Authorization: Bearer {token}`. Removed username TextInput — user email shown instead. Added Sign out button (top-right, red). Added transparent 401 → refresh → retry flow. Added STT/TTS/n8n preflight checks to the authenticated session/start handler in livekit.ts so health warnings carry over from the starter endpoint.
