---
phase: 03-provider-adapter-system
plan: 02
type: execute
wave: 1
depends_on: [03-01]
files_modified:
  - apps/voice-agent/src/voice_agent/providers/__init__.py
  - apps/voice-agent/src/voice_agent/providers/config.py
  - apps/voice-agent/src/voice_agent/providers/adapter.py
  - apps/voice-agent/src/voice_agent/providers/openai_llm_adapter.py
  - apps/voice-agent/src/voice_agent/providers/openai_tts_adapter.py
  - apps/voice-agent/src/voice_agent/providers/openai_stt_adapter.py
  - apps/voice-agent/tests/test_adapters.py
autonomous: true
requirements: [ADAPT-01, ADAPT-02, ADAPT-03, ADAPT-04, ADAPT-05]

must_haves:
  truths:
    - "Factory functions produce OpenAI-compatible plugin instances as the default path for LLM"
    - "Factory functions fall back to HttpTTS/HttpSTT for non-OpenAI-compatible servers"
    - "Custom headers from profile config are injected into constructed provider instances"
    - "Custom body params from profile config are passed as extra_body (LLM) or body_params (HTTP adapters)"
    - "Custom endpoint paths route to HttpTTS/HttpSTT which support path overrides"
    - "Response field remapping works for non-standard provider responses"
    - "Config parser handles both old metadata schema and new Phase 2 schema"
  artifacts:
    - path: "apps/voice-agent/src/voice_agent/providers/config.py"
      provides: "Pydantic config models: ServiceConfig, LLMConfig, TTSConfig, STTConfig + parse functions"
      exports: ["ServiceConfig", "LLMConfig", "TTSConfig", "STTConfig", "parse_llm_config", "parse_tts_config", "parse_stt_config"]
    - path: "apps/voice-agent/src/voice_agent/providers/adapter.py"
      provides: "Core adapter utilities: build_openai_client, remap_response"
      exports: ["build_openai_client", "remap_response"]
    - path: "apps/voice-agent/src/voice_agent/providers/openai_llm_adapter.py"
      provides: "build_llm factory function"
      exports: ["build_llm"]
    - path: "apps/voice-agent/src/voice_agent/providers/openai_tts_adapter.py"
      provides: "build_tts factory function"
      exports: ["build_tts"]
    - path: "apps/voice-agent/src/voice_agent/providers/openai_stt_adapter.py"
      provides: "build_stt factory function"
      exports: ["build_stt"]
    - path: "apps/voice-agent/src/voice_agent/providers/__init__.py"
      provides: "Re-exports: build_llm, build_tts, build_stt, parse_*_config, remap_response"
      contains: "build_llm"
  key_links:
    - from: "apps/voice-agent/src/voice_agent/providers/openai_llm_adapter.py"
      to: "livekit.plugins.openai.LLM"
      via: "constructor with extra_headers and extra_body kwargs"
      pattern: "LLM\\(.*extra_headers"
    - from: "apps/voice-agent/src/voice_agent/providers/openai_tts_adapter.py"
      to: "apps/voice-agent/src/voice_agent/providers/http_tts.py"
      via: "fallback when server is non-OpenAI-compatible"
      pattern: "HttpTTS\\("
    - from: "apps/voice-agent/src/voice_agent/providers/openai_stt_adapter.py"
      to: "apps/voice-agent/src/voice_agent/providers/http_stt.py"
      via: "fallback when server is non-OpenAI-compatible"
      pattern: "HttpSTT\\("
    - from: "apps/voice-agent/src/voice_agent/providers/config.py"
      to: "metadata.providers dict"
      via: "parse_*_config functions normalize old and new schema"
      pattern: "parse_llm_config"
---

<objective>
Create the provider adapter module with typed config models, factory functions for LLM/TTS/STT, and the response remapping utility.

Purpose: This is the core of Phase 3 -- a config-driven adapter layer that sits between the entrypoint and the LiveKit/HTTP provider classes. It ensures dashboard profile overrides (headers, body params, endpoint paths) are applied at construction time.
Output: Six new Python files in providers/, all test stubs from Wave 0 now passing (unskipped).
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-provider-adapter-system/03-RESEARCH.md
@.planning/phases/03-provider-adapter-system/03-01-SUMMARY.md

<interfaces>
<!-- LiveKit OpenAI plugin constructors (from installed package inspection) -->
From livekit.plugins.openai (v1.4.4):
```python
class LLM:
    def __init__(self, *, model: str = "gpt-4o", api_key: str | None = None,
                 base_url: str | None = None, extra_headers: dict | None = None,
                 extra_body: dict | None = None, extra_query: dict | None = None,
                 temperature: float | None = None, ...): ...

class TTS:
    def __init__(self, *, model: str = "tts-1", voice: str = "alloy",
                 speed: float = 1.0, api_key: str | None = None,
                 base_url: str | None = None, client: openai.AsyncClient | None = None,
                 ...): ...

class STT:
    def __init__(self, *, model: str = "whisper-1", language: str | None = None,
                 api_key: str | None = None, base_url: str | None = None,
                 client: openai.AsyncClient | None = None, ...): ...
```

<!-- Existing HTTP adapters (from voice_agent/providers/) -->
From apps/voice-agent/src/voice_agent/providers/http_tts.py:
```python
class HttpTTS(TTS):
    def __init__(self, *, base_url: str, synth_path: str = "/tts",
                 auth_header: str | None = None, engine: str = "f5-tts",
                 language: str = "en", text_field_name: str = "text",
                 body_params: dict | None = None, query_params: dict | None = None,
                 sample_rate: int = 24000, num_channels: int = 1) -> None:
```

From apps/voice-agent/src/voice_agent/providers/http_stt.py:
```python
class HttpSTT(STT):
    def __init__(self, *, base_url: str, transcribe_path: str = "/v1/audio/transcriptions",
                 model: str = "whisper-1", language: str = "en",
                 response_format: str = "verbose_json",
                 auth_header: str | None = None, query_params: dict | None = None) -> None:
```

<!-- Current entrypoint _build_* functions (lines 878-1031 of entrypoint.py) -->
Current _build_stt: reads providers["stt"] with fields baseUrl, transcribePath, model, responseFormat, authHeader, queryParams
Current _build_llm: reads providers["llm"] with fields baseUrl, model, authHeader; and providers["n8n"] for N8N path
Current _build_tts: reads providers["tts"] with fields baseUrl, engine, synthPath, authHeader, textFieldName, bodyParams, queryParams
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Pydantic config models and parse functions</name>
  <files>apps/voice-agent/src/voice_agent/providers/config.py</files>
  <read_first>
    - apps/voice-agent/src/voice_agent/entrypoint.py (lines 878-1031)
    - apps/voice-agent/tests/conftest.py
    - apps/voice-agent/tests/test_adapters.py
    - apps/api/src/services/resolve-providers.ts
  </read_first>
  <behavior>
    - parse_llm_config(providers_old_schema) returns LLMConfig with base_url, model, api_key populated from old fields (baseUrl, model, authHeader -> api_key)
    - parse_llm_config(providers_new_schema) returns LLMConfig with custom_headers, custom_body_params, endpoint_path populated
    - parse_tts_config(providers_old_schema) returns TTSConfig with engine, synth_path (from synthPath), text_field_name, body_params (from bodyParams)
    - parse_tts_config(providers_new_schema) returns TTSConfig with custom_headers, custom_body_params, endpoint_path
    - parse_stt_config(providers_old_schema) returns STTConfig with transcribe_path (from transcribePath), auth_header (from authHeader)
    - parse_stt_config(providers_new_schema) returns STTConfig with custom_headers, endpoint_path, custom_body_params
  </behavior>
  <action>
Create `apps/voice-agent/src/voice_agent/providers/config.py` with:

1. `ServiceConfig(BaseModel)` — base class with fields:
   - `base_url: str = ""`
   - `api_key: str = ""`
   - `model: str = ""`
   - `endpoint_path: str = ""`
   - `custom_headers: dict[str, str] = {}`
   - `custom_body_params: dict = {}`
   - `response_field_map: dict[str, str] = {}`

2. `LLMConfig(ServiceConfig)` — adds nothing extra, defaults `model="llama3.1:8b"`, `endpoint_path="/v1/chat/completions"`.

3. `TTSConfig(ServiceConfig)` — adds:
   - `voice: str = ""`
   - `speed: float = 1.0`
   - `engine: str = "f5-tts"`
   - `language: str = "en"`
   - `text_field_name: str = "text"`
   - Default `endpoint_path="/tts"`

4. `STTConfig(ServiceConfig)` — adds:
   - `language: str = "en"`
   - `response_format: str = "verbose_json"`
   - Default `endpoint_path="/v1/audio/transcriptions"`

5. Parse functions that handle BOTH old and new schema:

   `parse_llm_config(providers: dict) -> LLMConfig`:
   - Read `providers.get("llm", {})`
   - Map old fields: `baseUrl` -> `base_url`, `authHeader` -> extract api_key (strip "Bearer " prefix)
   - Map new fields: `apiKey` -> `api_key`, `customHeaders` -> `custom_headers`, `customBodyParams` -> `custom_body_params`, `endpointPath` -> `endpoint_path`, `responseFieldMap` -> `response_field_map`
   - Prefer new field names when both present

   `parse_tts_config(providers: dict) -> TTSConfig`:
   - Read `providers.get("tts", {})`
   - Map old: `synthPath` -> `endpoint_path`, `authHeader` -> custom_headers["Authorization"], `bodyParams` -> `custom_body_params`, `textFieldName` -> `text_field_name`
   - Map new: `endpointPath` -> `endpoint_path`, `customHeaders` -> `custom_headers`, `customBodyParams` -> `custom_body_params`, `apiKey` -> `api_key`
   - Copy through: `engine`, `voice`, `baseUrl` -> `base_url`

   `parse_stt_config(providers: dict) -> STTConfig`:
   - Read `providers.get("stt", {})`
   - Map old: `transcribePath` -> `endpoint_path`, `authHeader` -> custom_headers["Authorization"], `queryParams` -> (ignore for now, HttpSTT handles it)
   - Map new: `endpointPath` -> `endpoint_path`, `customHeaders` -> `custom_headers`, `customBodyParams` -> `custom_body_params`, `apiKey` -> `api_key`
   - Copy through: `model`, `baseUrl` -> `base_url`, `language`, `responseFormat` -> `response_format`

After creating the file, unskip and update `test_parse_config_old_schema` and `test_parse_config_new_schema` in test_adapters.py to actually import from config.py and run assertions.
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2/apps/voice-agent && python -m pytest tests/test_adapters.py::test_parse_config_old_schema tests/test_adapters.py::test_parse_config_new_schema -x -v --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - config.py exports ServiceConfig, LLMConfig, TTSConfig, STTConfig, parse_llm_config, parse_tts_config, parse_stt_config
    - parse_llm_config handles both old (authHeader) and new (apiKey, customHeaders) field names
    - parse_tts_config maps synthPath to endpoint_path (old) and endpointPath to endpoint_path (new)
    - parse_stt_config maps transcribePath to endpoint_path (old) and endpointPath to endpoint_path (new)
    - Both config parsing tests pass
  </acceptance_criteria>
  <done>Typed config models exist. Parse functions normalize both old and new metadata schemas. Config parsing tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create adapter utilities and factory functions for LLM, TTS, STT</name>
  <files>
    apps/voice-agent/src/voice_agent/providers/adapter.py,
    apps/voice-agent/src/voice_agent/providers/openai_llm_adapter.py,
    apps/voice-agent/src/voice_agent/providers/openai_tts_adapter.py,
    apps/voice-agent/src/voice_agent/providers/openai_stt_adapter.py,
    apps/voice-agent/src/voice_agent/providers/__init__.py,
    apps/voice-agent/tests/test_adapters.py
  </files>
  <read_first>
    - apps/voice-agent/src/voice_agent/providers/config.py (just created in Task 1)
    - apps/voice-agent/src/voice_agent/providers/http_tts.py
    - apps/voice-agent/src/voice_agent/providers/http_stt.py
    - apps/voice-agent/src/voice_agent/entrypoint.py (lines 878-1031)
    - apps/voice-agent/tests/test_adapters.py
    - apps/voice-agent/tests/conftest.py
  </read_first>
  <behavior>
    - build_llm(LLMConfig with custom_headers) produces LLM with extra_headers set
    - build_llm(LLMConfig with custom_body_params) produces LLM with extra_body set
    - build_tts(TTSConfig with engine="f5-tts") returns HttpTTS (non-OpenAI engine)
    - build_tts(TTSConfig with endpoint_path="/v1/audio/speech" and no engine) returns OpenAI TTS plugin
    - build_tts(TTSConfig with custom_headers) has headers injected via httpx client (OpenAI path) or auth_header (HTTP path)
    - build_tts(TTSConfig with custom_body_params) has body_params set (HTTP path) or warns (OpenAI path limited)
    - build_stt(STTConfig with custom endpoint_path="/transcribe") returns HttpSTT with transcribe_path="/transcribe"
    - build_stt(STTConfig default) returns HttpSTT (since existing code always used HttpSTT when base_url present)
    - remap_response({"transcript": "hello"}, {"transcript": "text"}) returns {"text": "hello"}
    - remap_response({"text": "hello"}, {}) returns {"text": "hello"} unchanged
    - build_openai_client(base_url, api_key, extra_headers) returns openai.AsyncClient with httpx headers
  </behavior>
  <action>
1. Create `apps/voice-agent/src/voice_agent/providers/adapter.py`:
   - `build_openai_client(base_url: str, api_key: str, extra_headers: dict[str, str] | None = None) -> openai.AsyncClient`:
     Build httpx.AsyncClient with timeout (connect=15, read=30, write=5, pool=5), follow_redirects=True, headers=extra_headers. Return openai.AsyncClient(api_key=api_key, base_url=base_url, max_retries=0, http_client=http_client).
   - `remap_response(response: dict, field_map: dict[str, str]) -> dict`:
     If field_map empty, return response unchanged. Otherwise copy dict and for each (src, dst) in field_map, if src in dict, pop src and set dst.

2. Create `apps/voice-agent/src/voice_agent/providers/openai_llm_adapter.py`:
   - `build_llm(config: LLMConfig) -> LLM`:
     Import `from livekit.plugins.openai import LLM as OpenAILLM`.
     Build kwargs: model=config.model, api_key=config.api_key or "no-key", base_url=config.base_url.
     If config.custom_headers: kwargs["extra_headers"] = config.custom_headers.
     If config.custom_body_params: kwargs["extra_body"] = config.custom_body_params.
     Return OpenAILLM(**kwargs).

3. Create `apps/voice-agent/src/voice_agent/providers/openai_tts_adapter.py`:
   - `build_tts(config: TTSConfig) -> TTS`:
     Determine if OpenAI-compatible: `is_openai = config.endpoint_path in ("", "/v1/audio/speech") and config.text_field_name in ("input", "") and config.engine not in ("f5-tts", "xtts", "chatterbox")`.
     **OpenAI path:** use `build_openai_client(config.base_url, config.api_key, config.custom_headers)` to create client. Return `OpenAITTS(model=config.model or "tts-1", voice=config.voice or "alloy", speed=config.speed, client=client)`.
     **HTTP fallback path:** Return `HttpTTS(base_url=config.base_url, synth_path=config.endpoint_path or "/tts", auth_header=config.custom_headers.get("Authorization"), engine=config.engine, language=config.language, text_field_name=config.text_field_name, body_params=config.custom_body_params or None)`.

4. Create `apps/voice-agent/src/voice_agent/providers/openai_stt_adapter.py`:
   - `build_stt(config: STTConfig) -> STT`:
     Determine if OpenAI-compatible: `is_openai = config.endpoint_path in ("", "/v1/audio/transcriptions") and not config.response_field_map and not config.custom_headers`.
     Note: The current codebase always uses HttpSTT when base_url is present, and only falls back to OpenAI STT plugin from env vars. Keep HttpSTT as the primary path for backward compat, use OpenAI STT only when explicitly detected.
     **Simpler approach:** Always use HttpSTT when base_url is present (matches current behavior). If custom_headers has "Authorization", pass as auth_header. Pass endpoint_path as transcribe_path.
     Only use OpenAI STT plugin when there's NO base_url but we have api_key (direct OpenAI API usage).
     **HttpSTT path:** Return `HttpSTT(base_url=config.base_url, transcribe_path=config.endpoint_path, model=config.model, language=config.language, response_format=config.response_format, auth_header=config.custom_headers.get("Authorization"), query_params=None)`.
     **OpenAI path (api_key only):** Use `build_openai_client(config.base_url or "https://api.openai.com/v1", config.api_key, config.custom_headers)`. Return `OpenAISTT(model=config.model, language=config.language, client=client)`.

5. Update `apps/voice-agent/src/voice_agent/providers/__init__.py`:
   ```python
   from .config import (
       ServiceConfig, LLMConfig, TTSConfig, STTConfig,
       parse_llm_config, parse_tts_config, parse_stt_config,
   )
   from .adapter import build_openai_client, remap_response
   from .openai_llm_adapter import build_llm
   from .openai_tts_adapter import build_tts
   from .openai_stt_adapter import build_stt

   __all__ = [
       "ServiceConfig", "LLMConfig", "TTSConfig", "STTConfig",
       "parse_llm_config", "parse_tts_config", "parse_stt_config",
       "build_openai_client", "remap_response",
       "build_llm", "build_tts", "build_stt",
   ]
   ```

6. Unskip ALL remaining tests in `test_adapters.py` and update them to use real imports. Fix any assertion logic now that the actual adapter code exists. Ensure:
   - Tests that check LLM extra_headers inspect the `_opts` or constructor args of the returned OpenAILLM instance
   - Tests that check HttpTTS path verify `._synth_path`, `._auth_header`, `._body_params`
   - Tests that check HttpSTT path verify `._transcribe_path`, `._auth_header`
   - `test_response_remap` calls `remap_response()` directly
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2/apps/voice-agent && python -m pytest tests/test_adapters.py -x -v --timeout=30</automated>
  </verify>
  <acceptance_criteria>
    - All 12+ tests in test_adapters.py pass (no skips)
    - `from voice_agent.providers import build_llm, build_tts, build_stt` works
    - `from voice_agent.providers import parse_llm_config, parse_tts_config, parse_stt_config` works
    - `from voice_agent.providers import remap_response` works
    - build_llm with custom_headers produces LLM instance with those headers accessible
    - build_tts with engine="f5-tts" produces HttpTTS, not OpenAI TTS
    - build_stt with base_url produces HttpSTT with correct transcribe_path
    - remap_response correctly remaps field names
  </acceptance_criteria>
  <done>Complete adapter module with config parsing, factory functions, and response remapping. All adapter tests green.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/drascom/Work/coziyoo-v2/apps/voice-agent && python -m pytest tests/ -v --timeout=30
```
Expected: All tests pass including test_adapters.py (12+ tests), plus existing tests (test_actions_schema.py, etc.).
</verification>

<success_criteria>
- Six new files in providers/ (config.py, adapter.py, openai_llm_adapter.py, openai_tts_adapter.py, openai_stt_adapter.py, updated __init__.py)
- Config parser normalizes both old (authHeader, synthPath) and new (customHeaders, endpointPath) metadata fields
- LLM factory uses livekit-plugins-openai LLM with extra_headers/extra_body from profile config
- TTS factory routes to OpenAI TTS plugin or HttpTTS based on engine/endpoint compatibility
- STT factory routes to HttpSTT (primary) or OpenAI STT based on config
- remap_response utility handles field remapping for non-standard providers
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/03-provider-adapter-system/03-02-SUMMARY.md`
</output>
