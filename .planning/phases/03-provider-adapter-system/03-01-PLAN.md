---
phase: 03-provider-adapter-system
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - apps/voice-agent/pyproject.toml
  - apps/voice-agent/tests/__init__.py
  - apps/voice-agent/tests/conftest.py
  - apps/voice-agent/tests/test_adapters.py
autonomous: true
requirements: [ADAPT-01, ADAPT-02, ADAPT-03, ADAPT-04, ADAPT-05]

must_haves:
  truths:
    - "pytest runs and discovers test files in apps/voice-agent/tests/"
    - "Test stubs exist for every ADAPT requirement (01 through 05)"
    - "Shared fixtures provide mock metadata dicts in both old and new config shapes"
  artifacts:
    - path: "apps/voice-agent/tests/__init__.py"
      provides: "Package marker"
    - path: "apps/voice-agent/tests/conftest.py"
      provides: "Shared fixtures for mock provider metadata"
      contains: "mock_providers_new_schema"
    - path: "apps/voice-agent/tests/test_adapters.py"
      provides: "Test stubs for ADAPT-01 through ADAPT-05"
      contains: "test_build_llm_returns_openai"
    - path: "apps/voice-agent/pyproject.toml"
      provides: "pytest + pytest-asyncio config"
      contains: "tool.pytest.ini_options"
  key_links: []
---

<objective>
Set up pytest infrastructure and create failing test stubs for all five ADAPT requirements.

Purpose: Wave 0 test scaffolding must exist before any adapter code is written (Wave 1). Tests define the contract that adapters must satisfy.
Output: Runnable pytest suite with 8-10 failing test stubs covering ADAPT-01 through ADAPT-05.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-provider-adapter-system/03-RESEARCH.md

<interfaces>
<!-- Current metadata shape consumed by _build_* functions (old schema) -->
From apps/api/src/services/resolve-providers.ts:
```typescript
export type ResolvedProviders = {
  stt: {
    provider: string;
    baseUrl: string | null;
    transcribePath: string;
    model: string;
    queryParams: Record<string, string>;
    authHeader: string | null;
  };
  tts: {
    engine: string;
    baseUrl: string | null;
    synthPath: string;
    textFieldName: string;
    bodyParams: Record<string, unknown>;
    queryParams: Record<string, string>;
    authHeader: string | null;
  };
  n8n: {
    baseUrl: string | null;
    workflowId: string | null;
    mcpWorkflowId: string | null;
    webhookUrl: string | null;
    webhookPath: string | null;
    mcpWebhookPath: string | null;
    authHeader: string | null;
  };
};
```

<!-- New profile config shape (Phase 2 agent_profiles JSONB columns) -->
Expected new metadata shape (post Phase 2 resolve-providers update):
```json
{
  "stt": {
    "baseUrl": "https://stt.example.com",
    "apiKey": "sk-...",
    "model": "whisper-1",
    "endpointPath": "/v1/audio/transcriptions",
    "customHeaders": {"X-Custom": "value"},
    "customBodyParams": {"language": "tr"},
    "responseFieldMap": {}
  },
  "tts": {
    "baseUrl": "https://tts.example.com",
    "apiKey": "",
    "model": "tts-1",
    "endpointPath": "/tts",
    "customHeaders": {},
    "customBodyParams": {"speed": 1.2},
    "voice": "alloy",
    "engine": "f5-tts",
    "textFieldName": "text"
  },
  "llm": {
    "baseUrl": "https://llm.example.com/v1",
    "apiKey": "sk-...",
    "model": "llama3.1:8b",
    "endpointPath": "/v1/chat/completions",
    "customHeaders": {"X-Provider-Key": "abc"},
    "customBodyParams": {"temperature": 0.7}
  },
  "n8n": {
    "baseUrl": "https://n8n.example.com",
    "workflowId": "abc123",
    "webhookPath": "/webhook/abc123",
    "mcpWorkflowId": "def456"
  }
}
```

From apps/voice-agent/src/voice_agent/providers/http_tts.py:
```python
class HttpTTS(TTS):
    def __init__(self, *, base_url: str, synth_path: str = "/tts",
                 auth_header: str | None = None, engine: str = "f5-tts",
                 language: str = "en", text_field_name: str = "text",
                 body_params: dict | None = None, query_params: dict | None = None,
                 sample_rate: int = 24000, num_channels: int = 1) -> None:
```

From apps/voice-agent/src/voice_agent/providers/http_stt.py:
```python
class HttpSTT(STT):
    def __init__(self, *, base_url: str, transcribe_path: str = "/v1/audio/transcriptions",
                 model: str = "whisper-1", language: str = "en",
                 response_format: str = "verbose_json",
                 auth_header: str | None = None, query_params: dict | None = None) -> None:
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pytest config and install dependencies</name>
  <files>apps/voice-agent/pyproject.toml</files>
  <read_first>
    - apps/voice-agent/pyproject.toml
  </read_first>
  <action>
1. Add `[tool.pytest.ini_options]` section to `apps/voice-agent/pyproject.toml`:
   ```toml
   [tool.pytest.ini_options]
   testpaths = ["tests"]
   asyncio_mode = "auto"
   timeout = 30
   ```

2. Add test dependencies to the `[project.optional-dependencies]` section:
   ```toml
   [project.optional-dependencies]
   test = ["pytest>=8.0", "pytest-asyncio>=0.24", "pytest-timeout>=2.3"]
   ```

3. Install test dependencies by running:
   ```bash
   cd apps/voice-agent && pip install -e ".[test]"
   ```

4. Verify pytest discovers the test directory:
   ```bash
   cd apps/voice-agent && python -m pytest --collect-only 2>&1 | head -5
   ```
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2/apps/voice-agent && python -m pytest --collect-only 2>&1 | grep -q "no tests ran\|collected\|test session starts"</automated>
  </verify>
  <acceptance_criteria>
    - pyproject.toml contains [tool.pytest.ini_options] with testpaths = ["tests"]
    - pyproject.toml contains asyncio_mode = "auto"
    - `python -m pytest --version` succeeds inside the voice-agent venv
  </acceptance_criteria>
  <done>pytest is configured and runnable in apps/voice-agent</done>
</task>

<task type="auto">
  <name>Task 2: Create conftest fixtures and test stubs for ADAPT-01 through ADAPT-05</name>
  <files>
    apps/voice-agent/tests/__init__.py,
    apps/voice-agent/tests/conftest.py,
    apps/voice-agent/tests/test_adapters.py
  </files>
  <read_first>
    - apps/voice-agent/src/voice_agent/entrypoint.py (lines 878-1031 — the _build_stt, _build_llm, _build_tts functions)
    - apps/voice-agent/src/voice_agent/providers/http_tts.py
    - apps/voice-agent/src/voice_agent/providers/http_stt.py
    - apps/voice-agent/tests/test_actions_schema.py (existing test style reference)
  </read_first>
  <action>
1. Create `apps/voice-agent/tests/__init__.py` — empty file.

2. Create `apps/voice-agent/tests/conftest.py` with these fixtures:
   - `mock_providers_old_schema() -> dict` — returns a providers dict using the OLD field names (`baseUrl`, `authHeader`, `synthPath`, `transcribePath`, `bodyParams`, `queryParams`, `engine`, `textFieldName`). Include realistic values for stt, tts, n8n keys.
   - `mock_providers_new_schema() -> dict` — returns a providers dict using the NEW field names from Phase 2 profiles (`baseUrl`, `apiKey`, `model`, `endpointPath`, `customHeaders`, `customBodyParams`, `responseFieldMap`, `voice`, `engine`, `textFieldName`). Include stt, tts, llm, n8n keys.
   - `mock_providers_with_custom_headers() -> dict` — new schema with `customHeaders: {"X-Custom-Auth": "Bearer test-token", "X-Request-ID": "test-123"}` on llm, tts, and stt.
   - `mock_providers_with_custom_body() -> dict` — new schema with `customBodyParams: {"temperature": 0.7, "top_p": 0.9}` on llm and `customBodyParams: {"speed": 1.5}` on tts.
   - `mock_providers_with_custom_path() -> dict` — new schema with `endpointPath: "/custom/completions"` on llm, `endpointPath: "/synthesize"` on tts, `endpointPath: "/transcribe"` on stt.
   - `mock_providers_n8n_only() -> dict` — only n8n key populated (no llm key), for testing N8N LLM path remains separate.

3. Create `apps/voice-agent/tests/test_adapters.py` with these test stubs. Each test should import from the NOT-YET-EXISTING module `voice_agent.providers.config` and `voice_agent.providers` (the factory functions). Each test body should call the factory and assert the result. Mark all tests `pytest.mark.skip(reason="Wave 0 stub — adapter module not yet created")`.

   Test functions:
   - `test_build_llm_returns_openai(mock_providers_new_schema)` — ADAPT-01: call `build_llm(config)`, assert result is instance of `livekit.plugins.openai.LLM` (or LoggingLLM wrapping it).
   - `test_build_tts_returns_http_tts_for_custom_engine(mock_providers_new_schema)` — ADAPT-01: call `build_tts(config)`, assert result is HttpTTS when engine is "f5-tts" (non-OpenAI).
   - `test_build_stt_returns_http_stt(mock_providers_new_schema)` — ADAPT-01: call `build_stt(config)`, assert result is HttpSTT or OpenAI STT depending on config.
   - `test_llm_custom_headers_applied(mock_providers_with_custom_headers)` — ADAPT-02: build LLM, assert `extra_headers` contains the custom headers.
   - `test_tts_custom_headers_applied(mock_providers_with_custom_headers)` — ADAPT-02: build TTS, assert custom headers are present in the constructed adapter.
   - `test_llm_custom_body_params(mock_providers_with_custom_body)` — ADAPT-03: build LLM, assert `extra_body` contains `{"temperature": 0.7, "top_p": 0.9}`.
   - `test_tts_custom_body_params(mock_providers_with_custom_body)` — ADAPT-03: build TTS, assert body_params includes `{"speed": 1.5}`.
   - `test_custom_endpoint_path_tts(mock_providers_with_custom_path)` — ADAPT-04: build TTS with `endpointPath: "/synthesize"`, assert HttpTTS._synth_path == "/synthesize".
   - `test_custom_endpoint_path_stt(mock_providers_with_custom_path)` — ADAPT-04: build STT with `endpointPath: "/transcribe"`, assert HttpSTT._transcribe_path == "/transcribe".
   - `test_response_remap(mock_providers_new_schema)` — ADAPT-05: call `remap_response({"transcript": "hello"}, {"transcript": "text"})`, assert result == `{"text": "hello"}`.
   - `test_parse_config_old_schema(mock_providers_old_schema)` — backward compat: parse old schema into typed config, assert fields are normalized.
   - `test_parse_config_new_schema(mock_providers_new_schema)` — parse new schema into typed config, assert fields match.

   Each test stub should have a clear docstring stating which ADAPT-XX requirement it validates.
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2/apps/voice-agent && python -m pytest tests/test_adapters.py --collect-only 2>&1 | grep "test_" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_adapters.py --collect-only` shows 12+ collected tests
    - conftest.py contains fixtures: mock_providers_old_schema, mock_providers_new_schema, mock_providers_with_custom_headers, mock_providers_with_custom_body, mock_providers_with_custom_path, mock_providers_n8n_only
    - All tests are marked skip (Wave 0 stubs)
    - test_adapters.py imports reference `voice_agent.providers.config` and `voice_agent.providers` (will fail on import until Wave 1)
  </acceptance_criteria>
  <done>12+ test stubs exist covering all 5 ADAPT requirements, all currently skipped. pytest --collect-only discovers them all.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/drascom/Work/coziyoo-v2/apps/voice-agent && python -m pytest tests/ --collect-only -q
```
Expected: 12+ tests collected, 0 errors.
</verification>

<success_criteria>
- pytest runs in apps/voice-agent and discovers tests/test_adapters.py
- 12+ test stubs exist, one or more per ADAPT requirement
- conftest.py has fixtures for old schema, new schema, custom headers, custom body, custom path
- All stubs are skipped (no import errors from non-existent adapter module)
</success_criteria>

<output>
After completion, create `.planning/phases/03-provider-adapter-system/03-01-SUMMARY.md`
</output>
