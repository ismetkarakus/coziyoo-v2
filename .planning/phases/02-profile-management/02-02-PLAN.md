---
phase: 02-profile-management
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - apps/voice-agent/src/voice_agent/join_api.py
  - apps/voice-agent/src/voice_agent/dashboard_forms.py
  - apps/voice-agent/src/voice_agent/templates/profiles/index.html
  - apps/voice-agent/src/voice_agent/templates/profiles/_editor_model.html
  - apps/voice-agent/src/voice_agent/templates/profiles/_editor_voice.html
  - apps/voice-agent/src/voice_agent/templates/profiles/_editor_transcriber.html
  - apps/voice-agent/src/voice_agent/templates/profiles/_editor_tools.html
autonomous: true
requirements: [MODEL-01, MODEL-02, MODEL-03, MODEL-04, MODEL-05, MODEL-06, MODEL-07, VOICE-01, VOICE-02, VOICE-03, VOICE-04, VOICE-05, STT-01, STT-02, STT-03, STT-04, STT-05, TOOLS-01, TOOLS-02, TOOLS-03]

must_haves:
  truths:
    - "4-tab editor is available for selected profile: Model, Voice, Transcriber, Tools"
    - "All profile config fields map to agent_profiles schema and persist through PUT /v1/admin/agent-profiles/:id"
    - "Nested JSON fields (custom_headers, custom_body_params, custom_query_params) round-trip correctly"
  artifacts:
    - path: "apps/voice-agent/src/voice_agent/templates/profiles/_editor_model.html"
      provides: "MODEL-* fields including system prompt and greeting controls"
    - path: "apps/voice-agent/src/voice_agent/templates/profiles/_editor_voice.html"
      provides: "VOICE-* config fields excluding runtime test actions"
    - path: "apps/voice-agent/src/voice_agent/templates/profiles/_editor_transcriber.html"
      provides: "STT-* config fields excluding runtime test actions"
    - path: "apps/voice-agent/src/voice_agent/templates/profiles/_editor_tools.html"
      provides: "N8N base/webhook/mcp fields and save-ready payload slots"
    - path: "apps/voice-agent/src/voice_agent/dashboard_forms.py"
      provides: "Form normalization and JSON mapping for profile payloads"
  key_links:
    - from: "apps/voice-agent/src/voice_agent/join_api.py"
      to: "apps/api/src/routes/admin-agent-profiles.ts"
      via: "save endpoint proxies profile PUT payload"
      pattern: "PUT /v1/admin/agent-profiles/"
---

<objective>
Implement the complete 4-tab profile editor forms and save flow on the FastAPI dashboard.

Purpose: deliver full profile configuration capability with durable persistence against existing `agent_profiles` API schema.
Output: tabbed editor templates, normalized payload mapper, and save endpoint that persists all fields.
</objective>

<execution_context>
@/Users/drascom/.codex/get-shit-done/workflows/execute-plan.md
@/Users/drascom/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-profile-management/02-RESEARCH.md
@.planning/phases/02-profile-management/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build profile editor routing and shared form normalization helpers</name>
  <files>
    apps/voice-agent/src/voice_agent/join_api.py,
    apps/voice-agent/src/voice_agent/dashboard_forms.py,
    apps/voice-agent/src/voice_agent/templates/profiles/index.html
  </files>
  <read_first>
    - apps/voice-agent/src/voice_agent/join_api.py
    - apps/api/src/routes/admin-agent-profiles.ts
    - .planning/phases/02-profile-management/02-RESEARCH.md
  </read_first>
  <action>
1. Create `dashboard_forms.py` with concrete helper functions:
   - `parse_key_value_pairs(form_data, prefix)` -> `dict[str, str]`
   - `normalize_profile_payload(form_data)` -> dict matching agent_profiles API payload shape
   - defaults:
     - `llm_config.endpoint_path=/v1/chat/completions`
     - `tts_config.endpoint_path=/v1/audio/speech`
     - `stt_config.endpoint_path=/v1/audio/transcriptions`
     - `voice_language=tr`
2. In `join_api.py`, add profile detail route:
   - `GET /dashboard/profiles/{id}` loads profile and renders editor tabs.
3. Add save route:
   - `POST /dashboard/profiles/{id}/save` parses form using `normalize_profile_payload`, proxies `PUT /v1/admin/agent-profiles/{id}`, returns save status partial.
4. Update `profiles/index.html` so selecting sidebar item loads profile editor panel using HTMX (`hx-get=/dashboard/profiles/{id}`).
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2 && python -m py_compile apps/voice-agent/src/voice_agent/join_api.py apps/voice-agent/src/voice_agent/dashboard_forms.py</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard_forms.py` exports `normalize_profile_payload`
    - `join_api.py` contains `GET /dashboard/profiles/{id}` and `POST /dashboard/profiles/{id}/save`
    - Save handler proxies to `PUT /v1/admin/agent-profiles/{id}`
    - `profiles/index.html` includes HTMX-driven profile editor panel target
  </acceptance_criteria>
  <done>Editor routing and payload normalization pipeline are in place.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Model tab template and bindings (MODEL-01..07)</name>
  <files>
    apps/voice-agent/src/voice_agent/templates/profiles/_editor_model.html,
    apps/voice-agent/src/voice_agent/join_api.py
  </files>
  <read_first>
    - apps/api/src/routes/admin-agent-profiles.ts
    - .planning/REQUIREMENTS.md
    - apps/voice-agent/src/voice_agent/templates/profiles/index.html
  </read_first>
  <action>
1. Create `_editor_model.html` with exact field names aligned to API payload:
   - `llm_config.base_url`
   - `llm_config.api_key`
   - `llm_config.model`
   - `llm_config.endpoint_path`
   - key-value collections for `llm_config.custom_headers`
   - key-value collections for `llm_config.custom_body_params`
   - `system_prompt`
   - `speaks_first`
   - `greeting_enabled`
   - `greeting_instruction`
   - `voice_language`
2. Ensure current profile values prefill all fields.
3. Add this tab into profile editor layout and make sure save submits these fields under one form.
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2 && rg -n \"llm_config\\.base_url|speaks_first|greeting_enabled|system_prompt\" apps/voice-agent/src/voice_agent/templates/profiles/_editor_model.html -S</automated>
  </verify>
  <acceptance_criteria>
    - `_editor_model.html` includes all MODEL field names listed in action
    - Form includes key-value controls for `custom_headers` and `custom_body_params`
    - `speaks_first` control is present and bound
    - `greeting_instruction` field exists and saves when greeting is enabled
  </acceptance_criteria>
  <done>Model tab provides complete LLM + conversation behavior controls.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Voice/Transcriber/Tools tabs and save bindings</name>
  <files>
    apps/voice-agent/src/voice_agent/templates/profiles/_editor_voice.html,
    apps/voice-agent/src/voice_agent/templates/profiles/_editor_transcriber.html,
    apps/voice-agent/src/voice_agent/templates/profiles/_editor_tools.html,
    apps/voice-agent/src/voice_agent/join_api.py
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - apps/api/src/routes/admin-agent-profiles.ts
    - apps/voice-agent/src/voice_agent/dashboard_forms.py
  </read_first>
  <action>
1. Create `_editor_voice.html` with fields:
   - `tts_config.base_url`, `tts_config.api_key`, `tts_config.model`, `tts_config.endpoint_path`
   - `tts_config.voice_id`, `tts_config.text_field_name`
   - key-value controls for `tts_config.custom_headers`, `tts_config.custom_body_params`
2. Create `_editor_transcriber.html` with fields:
   - `stt_config.base_url`, `stt_config.api_key`, `stt_config.model`, `stt_config.endpoint_path`
   - `stt_config.language`
   - key-value controls for `stt_config.custom_headers`, `stt_config.custom_body_params`, `stt_config.custom_query_params`
3. Create `_editor_tools.html` with fields:
   - `n8n_config.base_url`, `n8n_config.webhook_path`, `n8n_config.mcp_webhook_path`
4. Wire all tab templates into editor panel so one save action persists every field category.
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2 && rg -n \"tts_config\\.voice_id|stt_config\\.custom_query_params|n8n_config\\.mcp_webhook_path\" apps/voice-agent/src/voice_agent/templates/profiles/_editor_*.html -S</automated>
  </verify>
  <acceptance_criteria>
    - Voice tab contains all VOICE-01..05 save fields
    - Transcriber tab contains all STT-01..05 save fields
    - Tools tab contains TOOLS-01..03 save fields
    - Submitting save updates llm/stt/tts/n8n configs in a single PUT payload
  </acceptance_criteria>
  <done>Voice, Transcriber, and Tools tabs are fully implemented for persistent configuration editing.</done>
</task>

</tasks>

<verification>
1. `python -m py_compile` for modified python modules
2. Manual:
   - Open `/dashboard/profiles`
   - select profile
   - edit fields across all 4 tabs
   - save and reload
   - verify persisted values from API
</verification>

<success_criteria>
- Complete 4-tab editor exists in FastAPI dashboard
- All MODEL/VOICE/STT/TOOLS configuration fields persist through profile save
- Nested key-value config objects round-trip correctly
</success_criteria>

