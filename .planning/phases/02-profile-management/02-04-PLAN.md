---
phase: 02-profile-management
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - apps/api/src/routes/livekit.ts
  - apps/api/src/services/agent-profile-runtime.ts
  - apps/api/src/routes/__tests__/livekit-active-profile-runtime.test.ts
  - apps/voice-agent/src/voice_agent/entrypoint.py
autonomous: true
gap_closure: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05
  - PROF-06
  - MODEL-01
  - MODEL-02
  - MODEL-03
  - MODEL-04
  - MODEL-05
  - MODEL-06
  - MODEL-07
  - VOICE-01
  - VOICE-02
  - VOICE-03
  - VOICE-04
  - VOICE-05
  - VOICE-06
  - STT-01
  - STT-02
  - STT-03
  - STT-04
  - STT-05
  - STT-06
  - TOOLS-01
  - TOOLS-02
  - TOOLS-03
  - TOOLS-04
  - TOOLS-05
must_haves:
  truths:
    - "Active profile selection is reflected in live call runtime without redeploy."
    - "The next voice call consumes provider settings from active agent_profiles instead of starter_agent_settings."
    - "Voice agent receives and uses runtime profile payload (LLM/TTS/STT/tools) from dispatch metadata."
  artifacts:
    - path: "apps/api/src/routes/livekit.ts"
      provides: "Session start path resolves active agent_profiles config and injects it into agent dispatch metadata"
      exports: ["livekitRouter"]
    - path: "apps/api/src/services/agent-profile-runtime.ts"
      provides: "Runtime profile resolution and normalization helper from agent_profiles rows"
      exports: ["resolveRuntimeProfileConfig"]
    - path: "apps/voice-agent/src/voice_agent/entrypoint.py"
      provides: "Runtime metadata consumption path for profile-driven providers"
      min_lines: 40
    - path: "apps/api/src/routes/__tests__/livekit-active-profile-runtime.test.ts"
      provides: "Regression tests proving active profile runtime wiring"
      min_lines: 80
  key_links:
    - from: "apps/api/src/routes/livekit.ts"
      to: "apps/api/src/services/agent-profile-runtime.ts"
      via: "resolveRuntimeProfileConfig call during session startup"
      pattern: "resolveRuntimeProfileConfig"
    - from: "apps/api/src/routes/livekit.ts"
      to: "apps/voice-agent/src/voice_agent/entrypoint.py"
      via: "dispatchAgentJoin metadata.providers payload"
      pattern: "providers"
    - from: "apps/voice-agent/src/voice_agent/entrypoint.py"
      to: "providers runtime builders"
      via: "_build_stt/_build_llm/_build_tts consume metadata-derived providers"
      pattern: "_build_(stt|llm|tts)"
---

<objective>
Close verifier gap #1 by wiring runtime call configuration to active `agent_profiles`, so profile activation affects the next voice call immediately.

Purpose: Profile activation is incomplete until live session startup consumes the active profile payload.
Output: Active profile runtime resolver + livekit session wiring + voice-agent metadata consumption + regression tests.
</objective>

<execution_context>
@/Users/drascom/.codex/get-shit-done/workflows/execute-plan.md
@/Users/drascom/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-profile-management/02-VERIFICATION.md
@.planning/phases/02-profile-management/02-03-SUMMARY.md
@apps/api/src/routes/livekit.ts
@apps/voice-agent/src/voice_agent/entrypoint.py

<interfaces>
From `apps/api/src/routes/livekit.ts`:
```typescript
type StartSessionInput = z.infer<typeof StartSessionSchema>; // includes deviceId/settingsProfileId
// dispatchAgentJoin is called from start-session and starter-session flows
```

From `apps/voice-agent/src/voice_agent/entrypoint.py`:
```python
metadata_data = json.loads(ctx.job.metadata or "{}")
providers = metadata_data.get("providers", {})
stt_instance = _build_stt(providers, language)
llm_instance = _build_llm(providers, runtime_ctx=...)
tts_instance = _build_tts(providers, language)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add active-profile runtime resolver and API regression tests</name>
  <files>apps/api/src/services/agent-profile-runtime.ts, apps/api/src/routes/__tests__/livekit-active-profile-runtime.test.ts</files>
  <read_first>
    - apps/api/src/routes/livekit.ts
    - apps/api/src/routes/__tests__/agent-profiles.test.ts
    - apps/api/src/services/resolve-providers.js
    - apps/api/src/services/starter-agent-settings.ts
  </read_first>
  <behavior>
    - Test 1: When one profile has `is_active=true`, resolver returns normalized providers from that row.
    - Test 2: `settingsProfileId` override resolves the requested profile row and takes precedence.
    - Test 3: Missing active profile returns `null` (caller handles fallback/error path explicitly).
  </behavior>
  <action>Create `resolveRuntimeProfileConfig` that reads `agent_profiles` (`id`, `is_active`, config JSONB fields, prompt/greeting/language flags), normalizes the payload into the same runtime provider contract consumed by dispatch metadata, and add route-level tests that pin the resolver output and selection precedence (explicit profile id first, otherwise active profile).</action>
  <verify>
    <automated>npm run test --workspace=apps/api -- --run src/routes/__tests__/livekit-active-profile-runtime.test.ts</automated>
  </verify>
  <acceptance_criteria>Resolver output includes normalized `providers`, `voiceLanguage`, `systemPrompt`, `greetingEnabled`, and `greetingInstruction` from the selected `agent_profiles` row.</acceptance_criteria>
  <done>Runtime profile resolver is deterministic and covered by failing-then-passing tests for active/override/missing cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire live session startup to active agent_profiles payload</name>
  <files>apps/api/src/routes/livekit.ts, apps/api/src/routes/__tests__/livekit-active-profile-runtime.test.ts</files>
  <read_first>
    - apps/api/src/routes/livekit.ts
    - apps/api/src/services/livekit.ts
    - apps/api/src/services/agent-profile-runtime.ts
  </read_first>
  <behavior>
    - Test 1: Start-session metadata sent to `dispatchAgentJoin` includes profile-derived provider config.
    - Test 2: If profile resolution fails, endpoint returns explicit error instead of silently using stale starter settings.
    - Test 3: Runtime payload includes profile id for traceability.
  </behavior>
  <action>Update session-start paths in `livekit.ts` to resolve runtime config from `agent_profiles` (prefer `settingsProfileId`, fallback to active profile), inject the resolved object into dispatch metadata, and stop using starter-agent provider resolution for these runtime providers. Keep backward compatibility for non-provider fields that are still needed from existing flow.</action>
  <verify>
    <automated>npm run test --workspace=apps/api -- --run src/routes/__tests__/livekit-active-profile-runtime.test.ts src/routes/__tests__/agent-profiles.test.ts</automated>
  </verify>
  <acceptance_criteria>A profile activated in dashboard becomes the provider source for the next call without code redeploy.</acceptance_criteria>
  <done>Live session dispatch payload is profile-driven and test coverage guards against regressions.</done>
</task>

<task type="auto">
  <name>Task 3: Ensure voice-agent runtime consumes profile-driven metadata payload</name>
  <files>apps/voice-agent/src/voice_agent/entrypoint.py</files>
  <read_first>
    - apps/voice-agent/src/voice_agent/entrypoint.py
    - apps/api/src/routes/livekit.ts
  </read_first>
  <action>Adjust metadata parsing in `entrypoint.py` so provider/runtime fields delivered from profile-based dispatch are used as the primary source for `_build_stt/_build_llm/_build_tts` and prompt/greeting settings, while preserving safe fallback behavior for malformed metadata.</action>
  <verify>
    <automated>cd apps/voice-agent && python -m compileall src/voice_agent/entrypoint.py</automated>
  </verify>
  <acceptance_criteria>Voice-agent starts with provider values from metadata generated from active profile selection and no syntax/runtime import errors are introduced.</acceptance_criteria>
  <done>Runtime profile payload is consumed directly in voice-agent startup path.</done>
</task>

</tasks>

<verification>
Run API regression tests for profile-driven session start and run Python compile check for entrypoint metadata consumption path.
</verification>

<success_criteria>
1. Starting a new voice session after changing active profile uses that profile's provider configuration.
2. Dispatch metadata includes resolved profile id + provider payload.
3. Voice-agent consumes metadata profile payload without fallbacking to stale starter provider config.
</success_criteria>

<output>
After completion, create `.planning/phases/02-profile-management/02-04-SUMMARY.md`
</output>
