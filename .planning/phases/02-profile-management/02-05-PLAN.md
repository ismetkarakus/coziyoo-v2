---
phase: 02-profile-management
plan: 05
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - apps/api/src/routes/admin-livekit.ts
  - apps/api/src/routes/__tests__/admin-livekit-llm-test.test.ts
  - apps/voice-dashboard/src/lib/hooks/use-connection-test.ts
  - apps/voice-dashboard/src/components/tabs/model-tab.tsx
  - apps/voice-dashboard/src/components/forms/connection-test.tsx
autonomous: true
gap_closure: true
requirements:
  - MODEL-01
  - MODEL-02
  - MODEL-03
  - MODEL-04
must_haves:
  truths:
    - "Model tab includes an explicit LLM test action."
    - "LLM test returns clear success/failure feedback in the dashboard."
    - "LLM test request uses model-tab OpenAI-compatible config (base URL, endpoint path, headers, body overrides)."
  artifacts:
    - path: "apps/api/src/routes/admin-livekit.ts"
      provides: "LLM connectivity test endpoint for dashboard"
      exports: ["adminLiveKitRouter"]
    - path: "apps/api/src/routes/__tests__/admin-livekit-llm-test.test.ts"
      provides: "Route-level tests for LLM test endpoint success/error paths"
      min_lines: 80
    - path: "apps/voice-dashboard/src/lib/hooks/use-connection-test.ts"
      provides: "testLlm method and normalized result state"
      exports: ["useConnectionTest"]
    - path: "apps/voice-dashboard/src/components/tabs/model-tab.tsx"
      provides: "Model tab LLM test trigger + inline feedback"
      min_lines: 100
  key_links:
    - from: "apps/voice-dashboard/src/components/tabs/model-tab.tsx"
      to: "apps/voice-dashboard/src/lib/hooks/use-connection-test.ts"
      via: "testLlm call with form values"
      pattern: "testLlm"
    - from: "apps/voice-dashboard/src/lib/hooks/use-connection-test.ts"
      to: "/v1/admin/livekit/test/llm"
      via: "request() POST endpoint invocation"
      pattern: "test/llm"
---

<objective>
Close verifier gap #2 by adding LLM connectivity testing to the Model tab with immediate user feedback.

Purpose: Connectivity checks are incomplete without LLM testing, so users cannot validate model provider setup from the dashboard.
Output: New API LLM test endpoint + dashboard hook + Model tab test action/feedback UI.
</objective>

<execution_context>
@/Users/drascom/.codex/get-shit-done/workflows/execute-plan.md
@/Users/drascom/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-profile-management/02-VERIFICATION.md
@.planning/phases/02-profile-management/02-03-SUMMARY.md
@apps/api/src/routes/admin-livekit.ts
@apps/voice-dashboard/src/lib/hooks/use-connection-test.ts
@apps/voice-dashboard/src/components/tabs/model-tab.tsx

<interfaces>
From `apps/voice-dashboard/src/lib/hooks/use-connection-test.ts`:
```typescript
testTts(input): Promise<TestResult>
testStt(input): Promise<TestResult>
testN8n(input): Promise<TestResult>
```

From `apps/voice-dashboard/src/components/tabs/model-tab.tsx`:
```typescript
// already owns llm_config fields: base_url, api_key, model, endpoint_path,
// custom_headers, custom_body_params
```

From `apps/api/src/routes/admin-livekit.ts`:
```typescript
// Existing pattern: POST /test/{provider} with normalized { ok, status, error? } style response
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add LLM connectivity test endpoint with route-level regression coverage</name>
  <files>apps/api/src/routes/admin-livekit.ts, apps/api/src/routes/__tests__/admin-livekit-llm-test.test.ts</files>
  <read_first>
    - apps/api/src/routes/admin-livekit.ts
    - apps/api/src/routes/__tests__/agent-profiles.test.ts
    - apps/api/src/middleware/auth.ts
  </read_first>
  <behavior>
    - Test 1: Valid request to `/v1/admin/livekit/test/llm` returns success payload with upstream status.
    - Test 2: Network/timeout/upstream failure returns non-2xx with explicit error payload.
    - Test 3: Request composition forwards endpoint path, headers, and body overrides from model config.
  </behavior>
  <action>Implement `POST /v1/admin/livekit/test/llm` in `admin-livekit.ts`. Input contract must include OpenAI-compatible model config fields (`baseUrl`, `endpointPath`, `apiKey`, `model`, `customHeaders`, `customBodyParams`, optional test prompt). Compose request using endpoint override + merged headers/body params and return structured success/failure response consistent with existing test endpoints.</action>
  <verify>
    <automated>npm run test --workspace=apps/api -- --run src/routes/__tests__/admin-livekit-llm-test.test.ts</automated>
  </verify>
  <acceptance_criteria>Endpoint can validate LLM connectivity with user-supplied overrides and returns deterministic, UI-friendly result fields.</acceptance_criteria>
  <done>API exposes tested LLM connectivity route consumed by dashboard.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Model tab LLM test action and feedback through connection-test hook</name>
  <files>apps/voice-dashboard/src/lib/hooks/use-connection-test.ts, apps/voice-dashboard/src/components/tabs/model-tab.tsx, apps/voice-dashboard/src/components/forms/connection-test.tsx</files>
  <read_first>
    - apps/voice-dashboard/src/lib/hooks/use-connection-test.ts
    - apps/voice-dashboard/src/components/tabs/model-tab.tsx
    - apps/voice-dashboard/src/components/forms/connection-test.tsx
    - apps/voice-dashboard/src/lib/schemas/profile.ts
  </read_first>
  <action>Add `testLlm` to `useConnectionTest`, calling `/v1/admin/livekit/test/llm` with values from `llm_config` fields in the current form state. In `model-tab.tsx`, add a clear "Test LLM" action reusing `ConnectionTest` status UI and render success/failure details inline (status + concise message). Ensure request payload includes endpoint path/header/body overrides from the form, not hardcoded defaults.</action>
  <verify>
    <automated>npm run typecheck --workspace=apps/voice-dashboard && npm run build --workspace=apps/voice-dashboard</automated>
  </verify>
  <acceptance_criteria>User can click Model-tab LLM test and immediately see pass/fail feedback driven by live endpoint response.</acceptance_criteria>
  <done>Model tab now supports complete provider test matrix (LLM + TTS + STT + N8N) with consistent feedback UX.</done>
</task>

</tasks>

<verification>
Run API route tests for `/test/llm` and build/typecheck voice-dashboard to ensure Model-tab integration is production-safe.
</verification>

<success_criteria>
1. Model tab offers LLM connectivity test action.
2. LLM test request honors all OpenAI-compatible override fields from current form values.
3. Success/failure state is visible in dashboard feedback without leaving the page.
</success_criteria>

<output>
After completion, create `.planning/phases/02-profile-management/02-05-SUMMARY.md`
</output>
