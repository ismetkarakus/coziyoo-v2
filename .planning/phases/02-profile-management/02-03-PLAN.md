---
phase: 02-profile-management
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - apps/voice-agent/src/voice_agent/join_api.py
  - apps/voice-agent/src/voice_agent/dashboard_api.py
  - apps/voice-agent/src/voice_agent/curl_parser.py
  - apps/voice-agent/src/voice_agent/templates/profiles/_editor_model.html
  - apps/voice-agent/src/voice_agent/templates/profiles/_editor_voice.html
  - apps/voice-agent/src/voice_agent/templates/profiles/_editor_transcriber.html
  - apps/voice-agent/src/voice_agent/templates/profiles/_editor_tools.html
  - apps/voice-agent/src/voice_agent/templates/profiles/_status.html
  - apps/voice-agent/tests/test_dashboard_profiles.py
autonomous: true
requirements: [VOICE-06, STT-06, TOOLS-04, TOOLS-05]

must_haves:
  truths:
    - "User can run LLM/TTS/STT/N8N connectivity tests from the dashboard and see explicit success/failure feedback"
    - "TTS test returns playable audio in the browser from same-origin dashboard route"
    - "STT test supports microphone recording, sends base64 payload to backend, and shows transcript text"
    - "cURL import auto-fills matching profile section fields (LLM/TTS/STT/N8N)"
  artifacts:
    - path: "apps/voice-agent/src/voice_agent/join_api.py"
      provides: "dashboard test endpoints and cURL import endpoint"
    - path: "apps/voice-agent/src/voice_agent/curl_parser.py"
      provides: "cURL tokenizer/parser utility for import mapping"
    - path: "apps/voice-agent/src/voice_agent/templates/profiles/_editor_tools.html"
      provides: "cURL import UI and N8N test action"
    - path: "apps/voice-agent/src/voice_agent/templates/profiles/_editor_voice.html"
      provides: "TTS test action and audio playback control"
    - path: "apps/voice-agent/src/voice_agent/templates/profiles/_editor_transcriber.html"
      provides: "STT recording/upload test action and transcript output"
  key_links:
    - from: "apps/voice-agent/src/voice_agent/join_api.py"
      to: "apps/api/src/routes/admin-livekit.ts"
      via: "test routes proxy to /v1/admin/livekit/test/*"
      pattern: "/v1/admin/livekit/test/"
---

<objective>
Complete interactive provider testing and cURL import for the FastAPI profile dashboard.

Purpose: close remaining Phase 2 runtime requirements by enabling real connection checks and one-step config import inside the new FastAPI/HTMX UI.
Output: LLM/TTS/STT/N8N test actions with user feedback, browser audio/transcript display, and cURL-driven config autofill.
</objective>

<execution_context>
@/Users/drascom/.codex/get-shit-done/workflows/execute-plan.md
@/Users/drascom/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-profile-management/02-RESEARCH.md
@.planning/phases/02-profile-management/02-02-PLAN.md

<interfaces>
From apps/api/src/routes/admin-livekit.ts:
- POST /v1/admin/livekit/test/llm
- POST /v1/admin/livekit/test/tts
- POST /v1/admin/livekit/test/stt
- POST /v1/admin/livekit/test/stt/transcribe
- POST /v1/admin/livekit/test/n8n
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dashboard-side test proxy endpoints and status rendering</name>
  <files>
    apps/voice-agent/src/voice_agent/join_api.py,
    apps/voice-agent/src/voice_agent/dashboard_api.py,
    apps/voice-agent/src/voice_agent/templates/profiles/_status.html
  </files>
  <read_first>
    - apps/voice-agent/src/voice_agent/join_api.py
    - apps/voice-agent/src/voice_agent/dashboard_api.py
    - apps/api/src/routes/admin-livekit.ts
  </read_first>
  <action>
1. In `join_api.py`, add same-origin dashboard handlers:
   - `POST /dashboard/test/llm`
   - `POST /dashboard/test/tts`
   - `POST /dashboard/test/stt`
   - `POST /dashboard/test/stt/transcribe`
   - `POST /dashboard/test/n8n`
2. Each handler must proxy to matching Express admin-livekit endpoint and map response into `_status.html` partial with explicit states: `testing`, `success`, `error`.
3. For TTS test, support returning audio body from API response and expose a same-origin playback URL or inline base64 data source for `<audio controls>`.
4. Ensure all handlers pass auth through BFF token helper and never expose raw api.coziyoo.com URL in HTML response.
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2 && rg -n \"POST /dashboard/test/(llm|tts|stt|n8n)\" apps/voice-agent/src/voice_agent/join_api.py -S</automated>
  </verify>
  <acceptance_criteria>
    - `join_api.py` contains all dashboard test routes listed in action
    - `_status.html` exists and renders success/error messages from test actions
    - TTS path includes browser-playable audio output strategy
    - No template or route emits `https://api.coziyoo.com` as browser fetch target
  </acceptance_criteria>
  <done>Provider test proxy routes and status feedback partial are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Implement cURL import parser and form autofill mapping</name>
  <files>
    apps/voice-agent/src/voice_agent/curl_parser.py,
    apps/voice-agent/src/voice_agent/join_api.py,
    apps/voice-agent/src/voice_agent/templates/profiles/_editor_tools.html,
    apps/voice-agent/src/voice_agent/templates/profiles/_editor_model.html,
    apps/voice-agent/src/voice_agent/templates/profiles/_editor_voice.html,
    apps/voice-agent/src/voice_agent/templates/profiles/_editor_transcriber.html
  </files>
  <read_first>
    - apps/admin/src/pages/VoiceAgentSettingsPage.tsx
    - apps/voice-agent/src/voice_agent/join_api.py
    - .planning/phases/02-profile-management/02-RESEARCH.md
  </read_first>
  <action>
1. Create `curl_parser.py` with concrete functions:
   - `tokenize_curl(raw: str) -> list[str]`
   - `parse_curl_command(raw: str) -> dict`
   Extract keys: `base_url`, `api_key`, `model`, `endpoint_path`, `custom_headers`, `custom_body_params`, optional `voice_id`, `text_field_name`.
2. Add `POST /dashboard/profiles/{id}/import-curl` route:
   - parse pasted cURL
   - determine target section by URL/path fingerprint:
     - contains `webhook` or `n8n` -> `n8n_config`
     - contains `transcri` -> `stt_config`
     - contains `speech` or `audio` -> `tts_config`
     - default -> `llm_config`
   - merge parsed values into existing profile config and persist via `PUT /v1/admin/agent-profiles/{id}`
3. Add cURL import UI in `_editor_tools.html`:
   - textarea input for raw cURL
   - HTMX post button to import route
   - status message showing which section was populated.
4. Ensure resulting editor re-renders with updated fields populated in corresponding tab templates.
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2 && rg -n \"import-curl|tokenize_curl|parse_curl_command\" apps/voice-agent/src/voice_agent -S</automated>
  </verify>
  <acceptance_criteria>
    - `curl_parser.py` exports `tokenize_curl` and `parse_curl_command`
    - `join_api.py` has `POST /dashboard/profiles/{id}/import-curl` route
    - Tools tab template includes cURL textarea + submit action
    - Import flow updates one of: llm_config, tts_config, stt_config, n8n_config
  </acceptance_criteria>
  <done>cURL import is functional and auto-fills matched profile config section.</done>
</task>

<task type="auto">
  <name>Task 3: Wire tab-level interactive tests (LLM/TTS/STT/N8N) and add regression tests</name>
  <files>
    apps/voice-agent/src/voice_agent/templates/profiles/_editor_model.html,
    apps/voice-agent/src/voice_agent/templates/profiles/_editor_voice.html,
    apps/voice-agent/src/voice_agent/templates/profiles/_editor_transcriber.html,
    apps/voice-agent/src/voice_agent/templates/profiles/_editor_tools.html,
    apps/voice-agent/tests/test_dashboard_profiles.py
  </files>
  <read_first>
    - apps/voice-agent/src/voice_agent/templates/profiles/_editor_model.html
    - apps/voice-agent/src/voice_agent/templates/profiles/_editor_voice.html
    - apps/voice-agent/src/voice_agent/templates/profiles/_editor_transcriber.html
    - apps/voice-agent/src/voice_agent/templates/profiles/_editor_tools.html
  </read_first>
  <action>
1. Add LLM test trigger in Model tab posting current `llm_config` to `/dashboard/test/llm` and render returned status partial.
2. Add TTS test UI in Voice tab:
   - test text input default: `Merhaba, bu bir test mesajidir.`
   - test button posts to `/dashboard/test/tts`
   - render `<audio controls>` on success
3. Add STT test UI in Transcriber tab:
   - record/stop controls via MediaRecorder in template script
   - convert audio blob to base64
   - post to `/dashboard/test/stt/transcribe`
   - render transcript text in status region
4. Add N8N test button in Tools tab posting to `/dashboard/test/n8n`.
5. Add `test_dashboard_profiles.py` regression tests covering:
   - unauthorized dashboard access redirects to `/dashboard/login`
   - profile list route calls BFF helper
   - import-curl handler route exists
   - test endpoint routes return status partial content type.
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2/apps/voice-agent && python -m pytest -q tests/test_dashboard_profiles.py</automated>
  </verify>
  <acceptance_criteria>
    - Model tab includes LLM test action endpoint reference
    - Voice tab includes TTS test action and audio playback control
    - Transcriber tab includes MediaRecorder-based recording flow and transcript output target
    - Tools tab includes N8N test action endpoint reference
    - `tests/test_dashboard_profiles.py` executes successfully
  </acceptance_criteria>
  <done>All remaining interactive test requirements are implemented with regression coverage.</done>
</task>

</tasks>

<verification>
1. `cd apps/voice-agent && python -m py_compile src/voice_agent/join_api.py src/voice_agent/curl_parser.py`
2. `cd apps/voice-agent && python -m pytest -q tests/test_dashboard_profiles.py`
3. Manual:
   - Run `/dashboard/profiles`
   - test LLM, TTS, STT, N8N buttons
   - confirm cURL import fills fields and save persists
</verification>

<success_criteria>
- VOICE-06, STT-06, TOOLS-04, TOOLS-05 are fully implemented in FastAPI dashboard
- Provider tests produce visible success/failure feedback
- TTS playback and STT transcript are demonstrably available in browser
- cURL import auto-fills matching config section and persists
</success_criteria>

