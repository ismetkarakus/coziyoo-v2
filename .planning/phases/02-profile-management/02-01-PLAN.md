---
phase: 02-profile-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/voice-agent/pyproject.toml
  - apps/voice-agent/src/voice_agent/join_api.py
  - apps/voice-agent/src/voice_agent/dashboard_api.py
  - apps/voice-agent/src/voice_agent/dashboard_auth.py
  - apps/voice-agent/src/voice_agent/templates/base.html
  - apps/voice-agent/src/voice_agent/templates/login.html
  - apps/voice-agent/src/voice_agent/templates/profiles/index.html
  - apps/voice-agent/src/voice_agent/templates/profiles/_sidebar.html
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, PROF-04, PROF-05, PROF-06]

must_haves:
  truths:
    - "Dashboard login/logout flow is served from FastAPI and stores admin tokens in HttpOnly cookies"
    - "Browser no longer calls api.coziyoo.com directly for profile CRUD operations"
    - "Profile sidebar list renders from /v1/admin/agent-profiles via server-side proxy"
    - "Create, activate, duplicate, and delete actions are available from the FastAPI dashboard routes"
  artifacts:
    - path: "apps/voice-agent/src/voice_agent/dashboard_api.py"
      provides: "Server-side API client for /v1/admin/* routes with bearer token forwarding"
    - path: "apps/voice-agent/src/voice_agent/dashboard_auth.py"
      provides: "Cookie session helpers and token refresh handling"
    - path: "apps/voice-agent/src/voice_agent/join_api.py"
      provides: "Dashboard auth + profile list/action routes"
    - path: "apps/voice-agent/src/voice_agent/templates/profiles/_sidebar.html"
      provides: "Sidebar with active profile indicator and profile actions"
  key_links:
    - from: "apps/voice-agent/src/voice_agent/join_api.py"
      to: "apps/voice-agent/src/voice_agent/dashboard_api.py"
      via: "join_api uses dashboard_api client helper"
      pattern: "from .dashboard_api import"
    - from: "apps/voice-agent/src/voice_agent/join_api.py"
      to: "apps/voice-agent/src/voice_agent/dashboard_auth.py"
      via: "join_api uses auth cookie/refresh helper"
      pattern: "from .dashboard_auth import"
---

<objective>
Build the FastAPI dashboard shell and BFF auth/session layer for profile management.

Purpose: establish a same-origin browser surface in voice-agent to eliminate CORS issues and make profile CRUD actions executable through HTMX endpoints.
Output: login page, authenticated profiles page with sidebar list and create/activate/duplicate/delete actions, all proxied server-side to existing Express admin APIs.
</objective>

<execution_context>
@/Users/drascom/.codex/get-shit-done/workflows/execute-plan.md
@/Users/drascom/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-profile-management/02-RESEARCH.md

<interfaces>
From apps/api/src/routes/admin-auth.ts:
- POST /v1/admin/auth/login
- POST /v1/admin/auth/refresh
- POST /v1/admin/auth/logout
- GET /v1/admin/auth/me

From apps/api/src/routes/admin-agent-profiles.ts:
- GET /v1/admin/agent-profiles
- POST /v1/admin/agent-profiles
- GET /v1/admin/agent-profiles/:id
- PUT /v1/admin/agent-profiles/:id
- DELETE /v1/admin/agent-profiles/:id
- POST /v1/admin/agent-profiles/:id/activate
- POST /v1/admin/agent-profiles/:id/duplicate
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dashboard template/runtime foundations in voice-agent</name>
  <files>
    apps/voice-agent/pyproject.toml,
    apps/voice-agent/src/voice_agent/join_api.py,
    apps/voice-agent/src/voice_agent/templates/base.html,
    apps/voice-agent/src/voice_agent/templates/login.html,
    apps/voice-agent/src/voice_agent/templates/profiles/index.html,
    apps/voice-agent/src/voice_agent/templates/profiles/_sidebar.html
  </files>
  <read_first>
    - apps/voice-agent/pyproject.toml
    - apps/voice-agent/src/voice_agent/join_api.py
    - apps/api/src/routes/admin-auth.ts
    - apps/api/src/routes/admin-agent-profiles.ts
  </read_first>
  <action>
1. Update `apps/voice-agent/pyproject.toml` dependencies to include `jinja2>=3.1` and `python-multipart>=0.0.9` for FastAPI form handling and server-rendered templates.
2. In `join_api.py`, initialize Jinja2 template rendering support and add dashboard page routes:
   - `GET /dashboard/login`
   - `GET /dashboard/profiles`
3. Create `base.html`, `login.html`, and `profiles/index.html` templates with HTMX and include `profiles/_sidebar.html` partial mounted in `#profiles-sidebar`.
4. In sidebar partial, render:
   - profile name
   - active state badge for `is_active=true`
   - action buttons for activate, duplicate, delete
   - create-profile form button
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2/apps/voice-agent && python -m py_compile src/voice_agent/join_api.py</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `jinja2` and `python-multipart` dependencies
    - `join_api.py` exposes `/dashboard/login` and `/dashboard/profiles` routes
    - `templates/profiles/_sidebar.html` contains active profile visual indicator (`is_active`)
    - `templates/login.html` exists and contains a POST form for admin login credentials
  </acceptance_criteria>
  <done>Dashboard template shell exists and renders login + profiles pages from FastAPI.</done>
</task>

<task type="auto">
  <name>Task 2: Implement cookie-based admin auth and server-side API proxy helpers</name>
  <files>
    apps/voice-agent/src/voice_agent/dashboard_api.py,
    apps/voice-agent/src/voice_agent/dashboard_auth.py,
    apps/voice-agent/src/voice_agent/join_api.py
  </files>
  <read_first>
    - apps/api/src/routes/admin-auth.ts
    - apps/voice-agent/src/voice_agent/join_api.py
    - apps/voice-agent/src/voice_agent/config/settings.py
  </read_first>
  <action>
1. Create `dashboard_api.py` with concrete helpers:
   - `api_request(method: str, path: str, access_token: str | None, json_body: dict | None = None) -> tuple[int, dict | bytes]`
   - Uses `API_BASE_URL` (env fallback `https://api.coziyoo.com`)
   - Adds `Authorization: Bearer {access_token}` header when token exists
2. Create `dashboard_auth.py` with concrete helpers:
   - cookie names: `coziyoo_admin_at`, `coziyoo_admin_rt`
   - `set_auth_cookies(response, access_token, refresh_token)`
   - `clear_auth_cookies(response)`
   - `refresh_access_token(refresh_token)` by calling `/v1/admin/auth/refresh`
3. Update `join_api.py` login/logout handlers:
   - `POST /dashboard/login` calls `/v1/admin/auth/login`, sets cookies, redirects to `/dashboard/profiles`
   - `POST /dashboard/logout` calls `/v1/admin/auth/logout`, clears cookies, redirects `/dashboard/login`
4. Add auth guard helper in `join_api.py`:
   - Reads `coziyoo_admin_at` cookie
   - If missing/expired, attempts refresh with `coziyoo_admin_rt`
   - Redirects to `/dashboard/login` if refresh fails
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2/apps/voice-agent && python -m py_compile src/voice_agent/dashboard_api.py src/voice_agent/dashboard_auth.py src/voice_agent/join_api.py</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard_auth.py` defines cookie constants `coziyoo_admin_at` and `coziyoo_admin_rt`
    - `join_api.py` has `POST /dashboard/login` and `POST /dashboard/logout` handlers
    - `join_api.py` includes refresh-on-demand path calling `/v1/admin/auth/refresh`
    - Browser-facing profile routes in `join_api.py` reject anonymous access with redirect to `/dashboard/login`
  </acceptance_criteria>
  <done>FastAPI dashboard auth/session layer is implemented with HttpOnly cookies and token refresh support.</done>
</task>

<task type="auto">
  <name>Task 3: Implement profile sidebar CRUD action endpoints via BFF proxy</name>
  <files>
    apps/voice-agent/src/voice_agent/join_api.py,
    apps/voice-agent/src/voice_agent/templates/profiles/_sidebar.html
  </files>
  <read_first>
    - apps/api/src/routes/admin-agent-profiles.ts
    - apps/voice-agent/src/voice_agent/join_api.py
    - apps/voice-agent/src/voice_agent/templates/profiles/_sidebar.html
  </read_first>
  <action>
1. Add FastAPI HTMX action routes:
   - `POST /dashboard/profiles` -> proxy `POST /v1/admin/agent-profiles`
   - `POST /dashboard/profiles/{id}/activate` -> proxy `POST /v1/admin/agent-profiles/{id}/activate`
   - `POST /dashboard/profiles/{id}/duplicate` -> proxy `POST /v1/admin/agent-profiles/{id}/duplicate`
   - `POST /dashboard/profiles/{id}/delete` -> proxy `DELETE /v1/admin/agent-profiles/{id}`
2. On each mutation, re-fetch profile list with `GET /v1/admin/agent-profiles` and re-render `profiles/_sidebar.html` partial.
3. Ensure delete action handles active-profile conflict explicitly:
   - surface API `409 CANNOT_DELETE_ACTIVE` message in rendered partial status area.
4. Keep all browser interactions same-origin (`/dashboard/*`) and never return api.coziyoo.com URLs to HTMX.
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2 && rg -n \"POST /dashboard/profiles|/activate|/duplicate|/delete\" apps/voice-agent/src/voice_agent/join_api.py -S</automated>
  </verify>
  <acceptance_criteria>
    - `join_api.py` contains all four mutation routes under `/dashboard/profiles`
    - `join_api.py` proxies to `/v1/admin/agent-profiles` endpoints (not direct DB writes)
    - Sidebar partial re-renders after create/activate/duplicate/delete actions
    - Delete-active conflict path references `CANNOT_DELETE_ACTIVE` or HTTP 409 response handling
  </acceptance_criteria>
  <done>Profile sidebar CRUD actions are functional through FastAPI BFF routes and HTMX partial refresh.</done>
</task>

</tasks>

<verification>
1. `cd apps/voice-agent && python -m py_compile src/voice_agent/join_api.py src/voice_agent/dashboard_api.py src/voice_agent/dashboard_auth.py`
2. Start join API and verify:
   - `/dashboard/login` loads
   - login redirects to `/dashboard/profiles`
   - create/activate/duplicate/delete update sidebar without CORS failures
</verification>

<success_criteria>
- Dashboard auth + profile list shell works from FastAPI same-origin routes
- CRUD sidebar actions are proxied to Express profile APIs
- Active profile indicator is visible in sidebar
- Browser does not issue direct cross-origin profile CRUD requests
</success_criteria>

