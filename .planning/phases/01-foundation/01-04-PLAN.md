---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/voice-dashboard/src/app/page.tsx
  - apps/voice-dashboard/src/app/login/page.tsx
  - apps/voice-dashboard/src/app/(dashboard)/layout.tsx
  - apps/voice-dashboard/src/app/(dashboard)/dashboard/page.tsx
  - apps/voice-dashboard/src/app/(dashboard)/page.tsx
autonomous: true
gap_closure: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
must_haves:
  truths:
    - "Login success redirects user to a reachable protected dashboard route"
    - "Authenticated dashboard shell is reachable at /dashboard after login"
    - "Logout can be triggered from reachable dashboard UI and returns user to /login"
  artifacts:
    - path: "apps/voice-dashboard/src/app/(dashboard)/dashboard/page.tsx"
      provides: "Reachable authenticated dashboard page at /dashboard"
      contains: "onLogout"
    - path: "apps/voice-dashboard/src/app/page.tsx"
      provides: "Root route forwarding to /dashboard"
      contains: "redirect(\"/dashboard\")"
    - path: "apps/voice-dashboard/src/app/login/page.tsx"
      provides: "Post-login navigation target update"
      contains: "router.push(\"/dashboard\")"
  key_links:
    - from: "apps/voice-dashboard/src/app/login/page.tsx"
      to: "apps/voice-dashboard/src/app/(dashboard)/dashboard/page.tsx"
      via: "router.push('/dashboard') after successful login"
      pattern: "router\\.push\\(\"/dashboard\"\\)"
    - from: "apps/voice-dashboard/src/app/page.tsx"
      to: "apps/voice-dashboard/src/app/(dashboard)/dashboard/page.tsx"
      via: "root redirect"
      pattern: "redirect\\(\"/dashboard\"\\)"
---

<objective>
Close the auth routing gap by making the protected dashboard shell reachable after login and keeping logout reachable from that shell.

Purpose: Verification failed because `/` always redirected to `/login`, which blocked post-login landing and made logout effectively unreachable in normal flow.
Output: Explicit reachable `/dashboard` route with existing AuthGuard shell, login redirect fixed to `/dashboard`, and root route forwarding to `/dashboard`.
</objective>

<execution_context>
@/Users/drascom/.codex/get-shit-done/workflows/execute-plan.md
@/Users/drascom/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VERIFICATION.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
From apps/voice-dashboard/src/app/login/page.tsx:
```typescript
const response = await request("/v1/admin/auth/login", { method: "POST", body: JSON.stringify({ email, password }) });
setTokens(json.data.tokens);
setAdmin(json.data.admin);
router.push("/");
```

From apps/voice-dashboard/src/app/page.tsx:
```typescript
export default function RootPage() {
  redirect("/login");
}
```

From apps/voice-dashboard/src/app/(dashboard)/layout.tsx:
```typescript
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <AuthGuard>{children}</AuthGuard>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create explicit protected /dashboard route and wire root redirect</name>
  <files>
    apps/voice-dashboard/src/app/(dashboard)/dashboard/page.tsx,
    apps/voice-dashboard/src/app/(dashboard)/layout.tsx,
    apps/voice-dashboard/src/app/page.tsx,
    apps/voice-dashboard/src/app/(dashboard)/page.tsx
  </files>
  <read_first>
    apps/voice-dashboard/src/app/page.tsx,
    apps/voice-dashboard/src/app/(dashboard)/page.tsx,
    apps/voice-dashboard/src/app/(dashboard)/layout.tsx,
    .planning/phases/01-foundation/01-VERIFICATION.md
  </read_first>
  <action>
1. Create `apps/voice-dashboard/src/app/(dashboard)/dashboard/page.tsx` by moving the current dashboard page implementation from `apps/voice-dashboard/src/app/(dashboard)/page.tsx` without changing logout behavior.
2. Keep `apps/voice-dashboard/src/app/(dashboard)/layout.tsx` as the protected shell wrapper with `AuthGuard` so `/dashboard` remains protected.
3. Update `apps/voice-dashboard/src/app/page.tsx` to `redirect("/dashboard")` (not `/login`).
4. Remove `apps/voice-dashboard/src/app/(dashboard)/page.tsx` so there is no stale unreachable root dashboard page.
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2 && npm run build --workspace=apps/voice-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `apps/voice-dashboard/src/app/page.tsx` contains `redirect("/dashboard")`
    - `apps/voice-dashboard/src/app/(dashboard)/dashboard/page.tsx` exists
    - `apps/voice-dashboard/src/app/(dashboard)/dashboard/page.tsx` contains `onLogout`
    - `apps/voice-dashboard/src/app/(dashboard)/layout.tsx` contains `AuthGuard`
    - `apps/voice-dashboard/src/app/(dashboard)/page.tsx` does not exist
    - `npm run build --workspace=apps/voice-dashboard` exits 0
  </acceptance_criteria>
  <done>Protected dashboard content is reachable at `/dashboard`, and root route forwards there instead of forcing `/login`.</done>
</task>

<task type="auto">
  <name>Task 2: Update login success navigation to /dashboard and keep logout path usable</name>
  <files>
    apps/voice-dashboard/src/app/login/page.tsx,
    apps/voice-dashboard/src/app/(dashboard)/dashboard/page.tsx
  </files>
  <read_first>
    apps/voice-dashboard/src/app/login/page.tsx,
    apps/voice-dashboard/src/app/(dashboard)/dashboard/page.tsx,
    apps/voice-dashboard/src/lib/api.ts
  </read_first>
  <action>
1. In `apps/voice-dashboard/src/app/login/page.tsx`, change post-login navigation from `router.push("/")` to `router.push("/dashboard")`.
2. Keep dashboard logout handler in `apps/voice-dashboard/src/app/(dashboard)/dashboard/page.tsx` as `await logout(); router.push("/login");` so logout remains directly testable from reachable UI.
3. Do not change token storage or refresh logic in `src/lib/auth.ts` and `src/lib/api.ts`; this plan only fixes route wiring.
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2 && grep -n 'router.push(\"/dashboard\")' apps/voice-dashboard/src/app/login/page.tsx && grep -n 'router.push(\"/login\")' 'apps/voice-dashboard/src/app/(dashboard)/dashboard/page.tsx' && npm run build --workspace=apps/voice-dashboard</automated>
  </verify>
  <acceptance_criteria>
    - `apps/voice-dashboard/src/app/login/page.tsx` contains `router.push("/dashboard")`
    - `apps/voice-dashboard/src/app/login/page.tsx` does not contain `router.push("/")`
    - `apps/voice-dashboard/src/app/(dashboard)/dashboard/page.tsx` contains `await logout()`
    - `apps/voice-dashboard/src/app/(dashboard)/dashboard/page.tsx` contains `router.push("/login")`
    - `npm run build --workspace=apps/voice-dashboard` exits 0
  </acceptance_criteria>
  <done>Login lands users on reachable protected dashboard route and logout from dashboard returns users to login.</done>
</task>

</tasks>

<verification>
1. `npm run build --workspace=apps/voice-dashboard` exits 0
2. `grep -n 'redirect("/dashboard")' apps/voice-dashboard/src/app/page.tsx` returns 1 match
3. `grep -n 'router.push("/dashboard")' apps/voice-dashboard/src/app/login/page.tsx` returns 1 match
4. `test -f apps/voice-dashboard/src/app/(dashboard)/dashboard/page.tsx` succeeds
5. `test ! -f apps/voice-dashboard/src/app/(dashboard)/page.tsx` succeeds
</verification>

<success_criteria>
- AUTH-01 gap closed: successful login can land on a reachable authenticated shell
- AUTH-03 gap closed: logout action is reachable from dashboard UI and redirects to `/login`
- AUTH-02 not regressed: build passes with existing token/refresh implementation unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
