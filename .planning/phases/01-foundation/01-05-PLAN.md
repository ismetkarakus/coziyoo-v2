---
phase: 01-foundation
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - apps/api/src/config/env.ts
  - apps/api/src/config/__tests__/env.cors-default.test.ts
autonomous: true
gap_closure: true
requirements:
  - APP-01
  - APP-02
  - APP-03
must_haves:
  truths:
    - "API fallback CORS default includes dashboard production origin and dashboard local dev origin"
    - "Fallback default change is protected by an automated regression test"
    - "API build and targeted test pass after fallback change"
  artifacts:
    - path: "apps/api/src/config/env.ts"
      provides: "CORS_ALLOWED_ORIGINS fallback string aligned with dashboard domains"
      contains: "https://agent.coziyoo.com"
    - path: "apps/api/src/config/__tests__/env.cors-default.test.ts"
      provides: "Regression test for fallback CORS origins"
      contains: "CORS_ALLOWED_ORIGINS"
  key_links:
    - from: "apps/api/src/config/env.ts"
      to: "dashboard origin access"
      via: "default fallback list when env var absent"
      pattern: "CORS_ALLOWED_ORIGINS"
    - from: "apps/api/src/config/__tests__/env.cors-default.test.ts"
      to: "apps/api/src/config/env.ts"
      via: "asserts fallback includes required origins"
      pattern: "agent\\.coziyoo\\.com"
---

<objective>
Close the CORS fallback gap by aligning API env-schema defaults with the production dashboard origin and enforcing that behavior with an automated regression test.

Purpose: Verification reported partial failure because env-schema fallback omitted `https://agent.coziyoo.com`; this can break APP-02 in fallback/default environments.
Output: Updated fallback default in `env.ts` plus a targeted test that fails if required dashboard origins are removed in future edits.
Contract: This is a post-01-01 CORS fallback transform fixup and must be applied after 01-01 baseline outputs exist.
</objective>

<execution_context>
@/Users/drascom/.codex/get-shit-done/workflows/execute-plan.md
@/Users/drascom/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VERIFICATION.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
From apps/api/src/config/env.ts:
```typescript
CORS_ALLOWED_ORIGINS: z
  .string()
  .default("http://localhost:8081,http://localhost:5173,http://localhost:19006,http://localhost:3001"),
```

Expected required origins after fix:
- `http://localhost:3001`
- `https://agent.coziyoo.com`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing CORS fallback regression test for required dashboard origins</name>
  <files>
    apps/api/src/config/__tests__/env.cors-default.test.ts
  </files>
  <read_first>
    apps/api/src/config/env.ts,
    apps/api/src/config/__tests__,
    apps/api/package.json
  </read_first>
  <behavior>
    - Test 1: With `process.env.CORS_ALLOWED_ORIGINS` unset, parsed fallback includes `http://localhost:3001`.
    - Test 2: With `process.env.CORS_ALLOWED_ORIGINS` unset, parsed fallback includes `https://agent.coziyoo.com`.
  </behavior>
  <action>
1. Create `apps/api/src/config/__tests__/env.cors-default.test.ts` with a focused unit test that imports/reloads `env.ts` in a controlled process env and asserts fallback CORS includes both required origins.
2. Make test deterministic: clear module cache or use dynamic import per test run so changes to `process.env` are respected.
3. Confirm test initially fails against the current 01-01 fallback (missing production dashboard origin) before changing `env.ts`.
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2 && npm run test --workspace=apps/api -- --run src/config/__tests__/env.cors-default.test.ts >/dev/null 2>&1; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/config/__tests__/env.cors-default.test.ts` exists
    - Test file contains `localhost:3001`
    - Test file contains `agent.coziyoo.com`
    - Test file contains an assertion against parsed `CORS_ALLOWED_ORIGINS` fallback
  </acceptance_criteria>
  <done>Regression test exists and encodes required fallback origins; it guards against future accidental removal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update API env fallback CORS default and make regression test pass</name>
  <files>
    apps/api/src/config/env.ts,
    apps/api/src/config/__tests__/env.cors-default.test.ts
  </files>
  <read_first>
    apps/api/src/config/env.ts,
    apps/api/src/config/__tests__/env.cors-default.test.ts,
    .planning/phases/01-foundation/01-VERIFICATION.md
  </read_first>
  <behavior>
    - Test 1: Fallback default includes both dashboard local and production origins.
    - Test 2: API build remains successful after env schema default update.
  </behavior>
  <action>
1. Apply post-01-01 fallback transform in `apps/api/src/config/env.ts`: update `CORS_ALLOWED_ORIGINS` `.default(...)` from `http://localhost:8081,http://localhost:5173,http://localhost:19006,http://localhost:3001` to include `https://agent.coziyoo.com` alongside existing localhost origins.
2. Keep change limited to the fallback default string; do not alter unrelated env keys or validation behavior.
3. Run the new targeted test and API build to confirm both pass.
  </action>
  <verify>
    <automated>cd /Users/drascom/Work/coziyoo-v2 && npm run test --workspace=apps/api -- --run src/config/__tests__/env.cors-default.test.ts && npm run build --workspace=apps/api</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/config/env.ts` CORS default string contains `https://agent.coziyoo.com`
    - `apps/api/src/config/env.ts` CORS default string contains `http://localhost:3001`
    - `npm run test --workspace=apps/api -- --run src/config/__tests__/env.cors-default.test.ts` exits 0
    - `npm run build --workspace=apps/api` exits 0
  </acceptance_criteria>
  <done>CORS fallback default is aligned with production dashboard origin and protected by passing automated test coverage.</done>
</task>

</tasks>

<verification>
1. `grep -n 'agent.coziyoo.com' apps/api/src/config/env.ts` returns a match in CORS default line
2. `npm run test --workspace=apps/api -- --run src/config/__tests__/env.cors-default.test.ts` exits 0
3. `npm run build --workspace=apps/api` exits 0
</verification>

<success_criteria>
- APP-02 gap closed: fallback CORS origin list includes dashboard production origin
- APP-01 and APP-03 remain green: API workspace still builds and regression test prevents fallback drift
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md`
</output>
